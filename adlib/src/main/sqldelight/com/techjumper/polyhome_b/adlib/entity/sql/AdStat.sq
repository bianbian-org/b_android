CREATE TABLE adstat (
  _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  adId INTEGER NOT NULL,
  count INTEGER NOT NULL,
  position INTEGER NOT NULL
);

select_all:
SELECT *
FROM adstat;

select_by_adid_and_type:
SELECT *
FROM adstat
WHERE adId = ?
AND position = ?;